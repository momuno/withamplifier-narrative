[project]
name = "taskapi"
version = "0.1.0"
description = "A simple task management API"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]
